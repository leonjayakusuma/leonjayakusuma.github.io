export interface Project {
    id: number
    title: string
    description: string
    bulletPoints?: string[]
    dateRange?: string
    technologies?: string[]
    demoUrl?: string
    githubUrl?: string
    imageUrl?: string
  }